# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment and configuration files (keep local, don't commit)
.env
config.json
data/

# Runtime history database (must never be committed: a `git reset --hard`
# deploy would otherwise overwrite live history). Set NAUTILUS_DATA_DIR to a
# persistent path outside the repo on the deployment.
history.db*

# Persisted network-scan results
last_scan_results.json

# Keep examples for setup guidance
!.env.example
!defaultConfig.json
